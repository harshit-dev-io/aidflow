rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // For Hackathon MVP: Allow read/write to authenticated users
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
